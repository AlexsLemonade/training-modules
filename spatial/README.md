# Spatial Transcriptomics Training Module

_This module is in active development._

Note that the `renv` environment can be temporarily used for development until the Docker image is bumped to R 4.5.
